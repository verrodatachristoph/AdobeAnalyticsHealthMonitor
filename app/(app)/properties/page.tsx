// Properties list — dense scannable table, agency-first.
// Spec: docs/features/overview.md (list derivative) + docs/design/design-framework.md

export default function PropertiesPage() {
  return (
    <section className="space-y-8">
      <header>
        <h1 className="text-4xl font-light tracking-tight">Properties</h1>
      </header>

      {/* TODO: top-bar filters (status, client, tag) */}
      {/* TODO: scannable table — one row per property, status column anchors each row */}
    </section>
  );
}
